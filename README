1. Initialize GNURadio submodules:
   >> git submodule update --init --recursive
2. Run the cbg_channelizer_file.py script, which stores the samples coming out of the channelizer gnuradio block.
   >> python python/cbg_channelizer_file.py
3. Compile the lora python module by running INSTALL.
   >> ./INSTALL
4. run the decoder script within python/ folder.
   >> python lora_decode.py